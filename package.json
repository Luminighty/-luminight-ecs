{
  "name": "@luminight/ecs",
  "version": "1.0.2",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -r -d dist/",
    "example": "node example.js",
    "build": "tsc",
    "test": "jest --runInBand",
    "test:dev": "jest --watchAll --runInBand",
    "prepublishOnly": "npm run clean && npm run build && npm test"
  },
  "repository": {
    "url": "https://github.com/Luminighty/luminight-ecs"
  },
  "author": "Luminight (http://luminight.dev/)",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
